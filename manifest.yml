applications:
- name: analytics-reporter-api
  instances: 1
  memory: 128M
  buildpacks:
      - nodejs_buildpack
  command: "chmod +x ./entrypoint.sh && ./entrypoint.sh"
  services:
  - analytics-reporter-database
  stack: cflinuxfs4
